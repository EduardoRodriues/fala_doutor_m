generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Documento {
  id        String   @id @default(uuid())
  conteudo  String
  embedding Unsupported("vector")
}

